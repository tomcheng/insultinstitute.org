{
  "name": "insultinstitute.org",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "homepage": "http://insultinstitute.org",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "react-scripts": "0.8.5"
  }
}
